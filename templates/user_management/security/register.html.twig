{% extends 'base.html.twig' %}

{% block title %}Créer un compte - AgriLink{% endblock %}

{% block body %}
{{ include('components/_auth_background.html.twig') }}
<div class="min-vh-100 d-flex align-items-center justify-content-center py-5 px-4 px-sm-5">
    <div class="w-100 mx-auto" style="max-width: 28rem;">
        <div>
            <div class="d-flex justify-content-center">
                <img src="{{ asset('logo_with_text.png') }}" alt="AgriLink Logo" class="logo logo-header">
            </div>
            <h2 class="mt-4 text-center display-5 fw-bold text-dark">
                Créer un compte AgriLink
            </h2>
            <p class="mt-2 text-center small text-muted">
                Ou
                <a href="{{ path('app_login') }}" class="fw-medium text-success text-decoration-none">
                    connectez-vous si vous avez déjà un compte
                </a>
            </p>
        </div>

        {{ form_start(registrationForm, {'attr': {'class': 'mt-4', 'novalidate': 'novalidate'}}) }}
            <div class="mb-4">
                <div class="mb-3">
                    {{ form_label(registrationForm.email) }}
                    {{ form_widget(registrationForm.email) }}
                    {{ form_errors(registrationForm.email) }}
                </div>

                <div class="mb-3">
                    {{ form_label(registrationForm.fullName) }}
                    {{ form_widget(registrationForm.fullName) }}
                    {{ form_errors(registrationForm.fullName) }}
                </div>

                <div class="mb-3">
                    {{ form_label(registrationForm.plainPassword.first) }}
                    {{ form_widget(registrationForm.plainPassword.first) }}
                    {{ form_errors(registrationForm.plainPassword.first) }}
                    <p class="mt-2 small text-muted">Minimum 6 caractères</p>
                </div>

                <div class="mb-3">
                    {{ form_label(registrationForm.plainPassword.second) }}
                    {{ form_widget(registrationForm.plainPassword.second) }}
                    {{ form_errors(registrationForm.plainPassword.second) }}
                </div>

                <div class="d-flex align-items-center mb-3">
                    {{ form_widget(registrationForm.agreeTerms) }}
                    {{ form_label(registrationForm.agreeTerms) }}
                </div>
                {{ form_errors(registrationForm.agreeTerms) }}
            </div>

            <div>
                <button type="submit" class="btn btn-success w-100 py-2 px-3">
                    Créer mon compte
                </button>
            </div>
        {{ form_end(registrationForm) }}
    </div>
</div>
{% endblock %}
