{% extends 'base.html.twig' %}
{% block body %}
<h1>Modifier l'annonce</h1>
{{ form_start(form) }}
{{ form_widget(form) }}
<button type="submit" class="btn btn-success">Enregistrer</button>
<a href="{{ path('annonce_index') }}" class="btn btn-secondary">Annuler</a>
{{ form_end(form) }}
{% endblock %}