<?php

namespace App\Service;

/**
 * Minimal GeminiService placeholder.
 *
 * This project auto-registers services from `src/` in `config/services.yaml`.
 * Some environments reference this service name, so we keep the class present
 * to allow the container to compile in `test` as well.
 */
final class GeminiService
{
}

