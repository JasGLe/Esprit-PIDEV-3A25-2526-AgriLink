<?php

namespace App\Service;

/**
 * Placeholder service.
 *
 * This class exists because Symfony autowiring scans `src/` and expects a
 * matching class for each PHP file. The project currently doesn't use it
 * directly, but an empty PHP file breaks the container compilation (tests too).
 */
final class GeminiService
{
}

